---
description: NDCPC archive — identity moved to cell-abca4 community hub
alwaysApply: true
---

# Keep current

NDCPC app code is being merged into **cell-master** as `/ndcpc/*` on the community hub.

- Canonical host: **`https://ndccommunity.vercel.app`**
- Legacy host: **`https://ndcpc.vercel.app`** → permanent redirect to `https://ndccommunity.vercel.app/ndcpc` (path-preserving; auth pages → hub `/login` / `/signup`)
- Shared Firebase: **`cell-abca4`** (client config + messaging SW updated)
- Collections: namespaced `ndcpc*` (see cell-master `src/lib/ndcpc/collections.ts`)
- FCM tokens: `users.fcmTokens[]` (not subcollection)
- Chat push: via community hub `/api/send-chat-push` (legacy Cloud Function optional)
- User ops: **`/users`** on the community hub
- Worship hub on hub: `/ndcpc/worship` (rosters / setlists / resources / order); Updates app at `/feedback`
- Legacy Firebase project `studio-7483951484-e5df7` — read-only after migration cutover
- Roster reminder cron runs on the community hub (`/api/cron/ndcpc-roster-reminders`), not this archive deploy

Do not add new NDCPC-only auth or signup flows here; port features into cell-master under `/ndcpc`.
