{
  "entities": {
    "Resource": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Resource",
      "type": "object",
      "description": "Represents a resource link in the directory, such as chants, songs, schedules, or announcements.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Resource entity."
        },
        "title": {
          "type": "string",
          "description": "Title of the resource."
        },
        "url": {
          "type": "string",
          "description": "URL of the resource.",
          "format": "uri"
        },
        "category": {
          "type": "string",
          "description": "The category of the resource (e.g., 'songs', 'chants')."
        },
        "description": {
          "type": "string",
          "description": "A brief description of the resource."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "The timestamp when the resource was created."
        }
      },
      "required": [
        "id",
        "title",
        "url",
        "category",
        "createdAt"
      ]
    },
    "RosterEntry": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "RosterEntry",
      "type": "object",
      "description": "Represents a single entry in the weekly volunteer/staff roster.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the RosterEntry entity."
        },
        "volunteerName": {
          "type": "string",
          "description": "Name of the volunteer or staff member."
        },
        "role": {
          "type": "string",
          "description": "The role assigned to the volunteer/staff for the week."
        },
        "date": {
          "type": "string",
          "description": "The date for which the roster entry is valid.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "volunteerName",
        "role",
        "date"
      ]
    },
    "Announcement": {
      "$schema": "http://json-schema.org/draft-07/schema#",
      "title": "Announcement",
      "type": "object",
      "description": "Represents an announcement to be displayed in the application.",
      "properties": {
        "id": {
          "type": "string",
          "description": "Unique identifier for the Announcement entity."
        },
        "title": {
          "type": "string",
          "description": "Title of the announcement."
        },
        "content": {
          "type": "string",
          "description": "The main text content of the announcement."
        },
        "date": {
          "type": "string",
          "description": "Date the announcement was created or is relevant to.",
          "format": "date-time"
        }
      },
      "required": [
        "id",
        "title",
        "content",
        "date"
      ]
    }
  },
  "auth": {
    "providers": [
      "password",
      "anonymous"
    ]
  },
  "firestore": {
    "structure": [
      {
        "path": "/resources/{resourceId}",
        "definition": {
          "entityName": "Resource",
          "schema": {
            "$ref": "#/backend/entities/Resource"
          },
          "description": "Stores all resources like songs and chants in a single collection. Includes 'resourceId' as document ID.",
          "params": [
            {
              "name": "resourceId",
              "description": "The unique identifier of the resource."
            }
          ]
        }
      },
      {
        "path": "/roster_entries/{rosterEntryId}",
        "definition": {
          "entityName": "RosterEntry",
          "schema": {
            "$ref": "#/backend/entities/RosterEntry"
          },
          "description": "Stores weekly volunteer/staff roster entries. Includes 'rosterEntryId' as document ID.",
          "params": [
            {
              "name": "rosterEntryId",
              "description": "The unique identifier of the roster entry."
            }
          ]
        }
      },
      {
        "path": "/announcements/{announcementId}",
        "definition": {
          "entityName": "Announcement",
          "schema": {
            "$ref": "#/backend/entities/Announcement"
          },
          "description": "Stores announcements. Includes 'announcementId' as document ID.",
          "params": [
            {
              "name": "announcementId",
              "description": "The unique identifier of the announcement."
            }
          ]
        }
      }
    ],
    "reasoning": "The Firestore data structure has been simplified to use a single top-level 'resources' collection for all resource types (songs, chants, etc.), distinguished by a 'category' field. This flat structure simplifies queries and data management, resolving issues with nested collections. Other collections like 'roster_entries' and 'announcements' remain as top-level collections for clarity and independent access control."
  }
}
